# 个人项目任务卡产品需求文档-PRD

| 项目名称 | 个人项目任务卡                                   |
| -------- | ------------------------------------------------ |
| 文档版本 | V1.0                                             |
| 调整日期 | 2026-09-03                                       |
| 产品定位 | 个人项目管理与任务可视化工具                     |
| 使用对象 | 仅当前用户本人                                   |
| 核心场景 | 用户管理自己的项目、拆解任务、跟踪进度、提醒完成 |

---

## 1. 调整说明

本方案由“团队任务卡”调整为“个人项目任务卡”。

### 1.1 与团队版的主要差异

| 模块       | 团队版                         | 个人版                   |
| ---------- | ------------------------------ | ------------------------ |
| 使用对象   | 多人协作                       | 仅用户本人               |
| 用户角色   | 管理员、负责人、参与人、查看者 | 无角色区分，仅当前用户   |
| 任务负责人 | 可指派他人                     | 默认且仅为用户本人       |
| 权限体系   | 需要复杂权限                   | 不需要，本人拥有全部权限 |
| 评论协作   | 需要评论、@、参与人            | 不需要，可改为个人备注   |
| 通知提醒   | 团队通知、指派通知             | 仅个人待办提醒、到期提醒 |
| 验收流程   | 可提交验收、驳回               | 不需要                   |
| 项目视角   | 团队项目、多人负载             | 个人项目、个人进度       |
| 核心价值   | 团队协作透明化                 | 个人工作规划、执行和复盘 |

---

## 2. 产品定位

```text
一个面向个人用户的项目管理与任务卡工具。
```

帮助用户：

1. 管理自己的项目；
2. 将项目拆解为任务卡；
3. 用看板方式可视化任务状态；
4. 跟踪任务优先级和截止时间；
5. 记录个人执行过程；
6. 查看今日待办和逾期任务；
7. 定期复盘项目进度和个人完成情况。

---

## 3. 产品目标

### 3.1 用户目标

用户希望通过这个工具解决：

1. 自己的项目和任务太多，容易忘记；
2. 任务散落在笔记、聊天、脑子里，缺少统一管理；
3. 不知道当前项目进展到哪一步；
4. 不知道今天应该优先做什么；
5. 任务缺少截止时间，容易拖延；
6. 完成后缺少记录，无法复盘。

### 3.2 产品目标

1. 让用户可以快速创建项目和任务；
2. 让用户用看板清晰看到任务状态；
3. 让用户明确每个任务的优先级和截止时间；
4. 让用户快速找到今天要做的事；
5. 让用户能跟踪每个项目的整体进度；
6. 让用户可以回顾已完成任务和项目成果。

### 3.3 核心衡量指标

| 指标             | 说明                          |
| ---------------- | ----------------------------- |
| 创建项目数       | 用户是否开始用项目管理事务    |
| 创建任务数       | 用户是否持续记录任务          |
| 任务完成率       | 已完成任务 / 总任务           |
| 今日任务完成率   | 今日完成任务 / 今日应完成任务 |
| 逾期任务数       | 未按期完成任务数量            |
| 连续使用天数     | 用户是否形成使用习惯          |
| 平均任务完成时长 | 从创建到完成的时间            |
| 看板打开频次     | 用户是否依赖看板查看任务      |

---

## 4. 用户对象

### 4.1 用户定义

本产品仅面向单一用户本人：

```text
当前登录用户 = 项目创建者 = 任务创建者 = 任务执行者 = 任务管理者
```

### 4.2 不使用角色权限

个人版不设置复杂角色，不需要：

- 管理员；
- 项目负责人；
- 任务负责人；
- 参与人；
- 验收人；
- 查看者。

所有数据默认仅用户本人可见、可编辑、可删除。

---

## 5. 核心使用场景

### 5.1 场景一：创建自己的项目

用户创建一个项目，例如：

- 找工作准备；
- 个人网站开发；
- 学习计划；
- 健身计划；
- 副业项目；
- 家庭装修；
- 旅行准备；
- 年度目标拆解。

用户可以填写项目名称、项目描述、计划开始时间、计划结束时间、项目颜色和项目状态。

---

### 5.2 场景二：把项目拆解成任务

用户进入项目后，将项目拆解为多个任务卡。

例如项目为“个人网站开发”：

```text
- 梳理网站栏目
- 设计首页结构
- 准备文案内容
- 开发首页
- 开发作品集页面
- 部署上线
- 检查移动端适配
```

每个任务卡可以设置优先级、截止时间、标签和备注。

---

### 5.3 场景三：用看板查看项目进度

用户进入项目看板，可以看到：

```text
待办
进行中
已完成
```

通过拖拽任务卡，用户可以更新任务状态。

例如：

```text
“梳理网站栏目”从待办拖到进行中；
“设计首页结构”从进行中拖到已完成。
```

---

### 5.4 场景四：查看今天需要做什么

用户打开“今日任务”页面，可以看到：

- 今天到期的任务；
- 已逾期未完成的任务；
- 自己标记为今天处理的任务；
- 进行中的高优先级任务。

帮助用户快速决定今天做什么。

---

### 5.5 场景五：任务到期提醒

用户为任务设置截止时间。

系统可以在截止前提醒用户：

- 截止前 1 天；
- 截止当天；
- 截止前 1 小时；
- 逾期后提醒。

提醒方式可根据端能力决定，例如：

- 站内提醒；
- App Push；
- 浏览器通知；
- 日历提醒；
- 本地通知。

---

### 5.6 场景六：项目复盘

用户可以查看项目统计：

- 总任务数；
- 已完成任务数；
- 进行中任务数；
- 逾期任务数；
- 项目完成进度；
- 最近完成任务；
- 长期未处理任务。

帮助用户复盘项目执行情况。

---

## 6. 需求范围

### 6.1 本期范围

个人版第一阶段建议做轻，但要闭环。

包含：

1. 项目管理；
2. 项目列表；
3. 项目详情；
4. 任务卡创建；
5. 任务卡编辑；
6. 任务看板；
7. 任务列表；
8. 今日任务；
9. 任务状态流转；
10. 优先级；
11. 截止时间；
12. 标签；
13. 任务备注；
14. 搜索；
15. 筛选；
16. 删除与回收站；
17. 基础提醒；
18. 项目进度统计。

### 6.2 暂不包含

以下能力可放到后续版本：

1. 多人协作；
2. 任务评论；
3. @提醒他人；
4. 任务分享；
5. 任务指派；
6. 团队权限；
7. 审批验收；
8. 工时统计；
9. 甘特图；
10. 日历视图；
11. 子任务；
12. 任务依赖；
13. 自动化规则；
14. 复杂自定义字段；
15. 团队报表。

---

## 7. 核心概念设计

个人版包含四个核心对象：

```text
项目
└── 任务卡
    ├── 状态
    ├── 优先级
    ├── 时间
    ├── 标签
    ├── 备注
    └── 附件/清单
```

### 7.1 项目

项目是任务的容器。

例如：

```text
个人网站
求职准备
英语学习
健身计划
副业探索
```

一个项目下可以有多张任务卡。

### 7.2 任务卡

任务卡是具体要做的事情。

例如：

```text
整理作品集内容
写简历初稿
背 30 个单词
完成首页设计
预约健身房体测
```

### 7.3 看板

看板用于展示任务状态。

默认三列：

```text
待办
进行中
已完成
```

### 7.4 今日任务

今日任务是个人执行视图，用于回答：

```text
我今天应该做什么？
哪些任务已经逾期？
哪些任务今天必须完成？
```

---

## 8. 产品架构

```text
个人项目任务卡
├── 项目列表
│   ├── 全部项目
│   ├── 进行中项目
│   ├── 已完成项目
│   ├── 已归档项目
│   └── 新建项目
├── 项目详情
│   ├── 项目看板
│   ├── 项目列表
│   ├── 项目统计
│   ├── 新建任务
│   └── 项目设置
├── 任务详情
│   ├── 基础信息
│   ├── 时间信息
│   ├── 标签
│   ├── 备注
│   ├── 附件
│   ├── 操作记录
│   └── 删除/复制/完成
├── 今日任务
│   ├── 今日到期
│   ├── 已逾期
│   ├── 计划今日
│   └── 进行中
├── 搜索与筛选
│   ├── 按项目
│   ├── 按状态
│   ├── 按优先级
│   ├── 按截止时间
│   └── 按标签
└── 设置
    ├── 提醒设置
    ├── 标签管理
    ├── 主题设置
    └── 数据备份
```

---

# 9. 功能需求详述

---

## 9.1 项目列表

### 9.1.1 功能目标

让用户查看和管理自己的所有项目。

### 9.1.2 页面结构

```text
项目列表页
├── 顶部操作区
│   ├── 搜索项目
│   ├── 筛选项目
│   └── 新建项目
├── 项目状态页签
│   ├── 全部
│   ├── 进行中
│   ├── 已完成
│   └── 已归档
└── 项目卡片
    ├── 项目名称
    ├── 项目颜色
    ├── 项目描述
    ├── 任务统计
    ├── 完成进度
    ├── 最近更新时间
    └── 项目状态
```

### 9.1.3 项目卡片展示字段

| 字段         | 说明                       |
| ------------ | -------------------------- |
| 项目名称     | 项目主标题                 |
| 项目颜色     | 用于区分项目               |
| 项目描述     | 简短说明，最多展示 1-2 行  |
| 总任务数     | 项目下任务总数             |
| 已完成任务数 | 已完成任务数量             |
| 进行中任务数 | 进行中任务数量             |
| 逾期任务数   | 未完成任务中已逾期数量     |
| 完成进度     | 已完成任务 / 总任务        |
| 最近更新时间 | 项目或任务最近一次更新时间 |
| 项目状态     | 进行中、已完成、已归档     |

### 9.1.4 项目操作

| 操作     | 说明                           |
| -------- | ------------------------------ |
| 新建项目 | 创建新项目                     |
| 进入项目 | 点击项目卡片进入项目详情       |
| 编辑项目 | 修改项目名称、描述、颜色、状态 |
| 归档项目 | 将项目归档，不再主动展示       |
| 删除项目 | 删除项目及其任务，需二次确认   |
| 置顶项目 | 常用项目置顶                   |
| 复制项目 | 可选，复制项目结构和任务       |

### 9.1.5 项目排序

默认排序：

```text
置顶项目在前
进行中项目在前
最近更新时间倒序
```

可支持手动排序。

---

## 9.2 新建项目

### 9.2.1 功能目标

让用户快速创建一个自己的项目。

### 9.2.2 入口

1. 项目列表页“新建项目”按钮；
2. 首页快捷入口；
3. 空状态页引导入口。

### 9.2.3 项目字段

| 字段         | 类型       | 是否必填 | 说明               |
| ------------ | ---------- | -------- | ------------------ |
| 项目名称     | 文本       | 是       | 建议最多 50 字     |
| 项目描述     | 文本       | 否       | 说明项目目标、背景 |
| 项目颜色     | 颜色选择   | 否       | 默认系统分配       |
| 项目图标     | 图标/Emoji | 否       | 可后续扩展         |
| 项目状态     | 单选       | 否       | 默认“进行中”       |
| 计划开始时间 | 日期       | 否       | 可选               |
| 计划结束时间 | 日期       | 否       | 可选               |
| 项目目标     | 文本       | 否       | 可后续扩展         |

### 9.2.4 校验规则

1. 项目名称不能为空；
2. 项目名称不能超过最大长度；
3. 若填写计划开始和结束时间，开始时间不能晚于结束时间；
4. 项目删除后不可在普通列表中展示，但可进入回收站恢复；
5. 项目数量较多时支持分页或懒加载。

### 9.2.5 创建成功后

1. 创建成功提示；
2. 用户可选择“继续创建”或“进入项目”；
3. 新项目默认状态为“进行中”；
4. 系统记录创建时间和更新时间。

---

## 9.3 项目详情

### 9.3.1 功能目标

展示某个项目下的任务，并支持用户创建、查看、编辑和拖拽任务。

### 9.3.2 页面结构

```text
项目详情页
├── 项目头部
│   ├── 返回项目列表
│   ├── 项目名称
│   ├── 项目状态
│   ├── 项目描述
│   ├── 项目统计
│   ├── 编辑项目
│   └── 新建任务
├── 视图切换
│   ├── 看板视图
│   └── 列表视图
├── 筛选区
│   ├── 状态
│   ├── 优先级
│   ├── 标签
│   ├── 截止时间
│   └── 是否逾期
└── 任务区域
    ├── 任务看板
    └── 任务列表
```

### 9.3.3 项目统计

项目详情顶部展示轻量统计：

| 统计项       | 说明                         |
| ------------ | ---------------------------- |
| 总任务数     | 当前项目任务总数             |
| 待办任务数   | 状态为待办的任务数           |
| 进行中任务数 | 状态为进行中的任务数         |
| 已完成任务数 | 状态为已完成的任务数         |
| 逾期任务数   | 未完成且超过截止时间的任务数 |
| 完成进度     | 已完成任务 / 总任务          |

### 9.3.4 项目视图

默认视图：

```text
看板视图
```

可切换：

```text
列表视图
```

后续可增加：

```text
日历视图
时间线视图
```

---

## 9.4 任务看板

### 9.4.1 功能目标

以可视化方式展示项目任务状态，帮助用户快速掌握项目进展。

### 9.4.2 默认看板列

| 状态列 | 说明                   |
| ------ | ---------------------- |
| 待办   | 任务已创建，但还没开始 |
| 进行中 | 任务正在处理           |
| 已完成 | 任务已经完成           |

### 9.4.3 可选扩展状态

如果用户希望更精细，可以增加：

| 状态   | 说明       |
| ------ | ---------- |
| 已暂停 | 暂时不推进 |
| 已取消 | 不再处理   |

但第一版建议保持三个状态，避免过重。

### 9.4.4 任务卡片展示

| 字段       | 是否展示 | 说明                 |
| ---------- | -------- | -------------------- |
| 任务标题   | 是       | 卡片主信息           |
| 优先级     | 是       | 高、中、低           |
| 截止时间   | 是       | 逾期时高亮           |
| 标签       | 是       | 最多展示 2-3 个      |
| 任务备注   | 否       | 可展示摘要           |
| 附件标识   | 否       | 有附件时展示图标     |
| 子任务进度 | 否       | 二期若有子任务再展示 |

### 9.4.5 看板操作

| 操作     | 说明                     |
| -------- | ------------------------ |
| 新建任务 | 在当前列快速新建任务     |
| 点击卡片 | 进入任务详情             |
| 拖拽卡片 | 修改任务状态或排序       |
| 筛选任务 | 按优先级、标签、时间筛选 |
| 搜索任务 | 搜索任务标题或备注       |
| 快捷完成 | 卡片上可支持勾选完成     |

### 9.4.6 拖拽规则

1. 用户可拖拽任务卡到不同状态列；
2. 拖拽后自动更新任务状态；
3. 拖拽到“已完成”时，系统记录完成时间；
4. 从“已完成”拖回“待办”或“进行中”时，清空或保留完成时间，建议清空；
5. 拖拽失败需提示并回滚；
6. 同一列内拖拽用于手动排序；
7. 可支持长按拖拽，尤其在移动端。

### 9.4.7 排序规则

默认排序建议：

```text
手动排序优先
逾期任务靠前
高优先级靠前
截止时间近靠前
更新时间倒序
```

用户可选择排序方式：

1. 手动排序；
2. 按截止时间；
3. 按优先级；
4. 按创建时间；
5. 按更新时间。

---

## 9.5 新建任务卡

### 9.5.1 功能目标

让用户快速创建一条个人任务。

### 9.5.2 入口

1. 项目详情页“新建任务”；
2. 看板列内“添加任务”；
3. 今日任务页“新建任务”；
4. 全局快捷创建入口；
5. 空状态引导入口。

### 9.5.3 任务字段

| 字段         | 类型      | 是否必填 | 默认值   | 说明                 |
| ------------ | --------- | -------- | -------- | -------------------- |
| 任务标题     | 文本      | 是       | 空       | 建议最多 100 字      |
| 任务描述     | 富文本    | 否       | 空       | 记录任务细节         |
| 所属项目     | 单选      | 是       | 当前项目 | 任务必须属于某个项目 |
| 状态         | 单选      | 否       | 待办     | 默认待办             |
| 优先级       | 单选      | 是       | 中       | 高、中、低           |
| 计划开始时间 | 日期      | 否       | 空       | 可选                 |
| 截止时间     | 日期/时间 | 否       | 空       | 建议填写             |
| 计划今日     | 开关      | 否       | 否       | 标记今天准备处理     |
| 标签         | 多选      | 否       | 空       | 用于分类             |
| 备注         | 文本      | 否       | 空       | 个人记录             |
| 附件         | 文件      | 否       | 空       | 可选                 |
| 提醒         | 开关/时间 | 否       | 关       | 可设置提醒           |

### 9.5.4 校验规则

1. 任务标题不能为空；
2. 任务标题不能超过最大长度；
3. 所属项目不能为空；
4. 优先级不能为空；
5. 若填写开始时间和截止时间，开始时间不能晚于截止时间；
6. 截止时间早于当前时间时，可提示但允许保存；
7. 附件超过大小限制时提示；
8. 任务删除后进入回收站。

### 9.5.5 创建成功后

1. 任务出现在对应看板列；
2. 若设置提醒，系统生成提醒计划；
3. 系统记录创建时间；
4. 项目更新时间同步更新；
5. 若用户在“今日任务”中创建并标记计划今日，则同步展示。

---

## 9.6 任务详情页

### 9.6.1 功能目标

查看和编辑任务完整信息。

### 9.6.2 页面结构

```text
任务详情页
├── 顶部区域
│   ├── 返回
│   ├── 任务标题
│   ├── 状态切换
│   ├── 完成按钮
│   └── 更多操作
├── 基础信息
│   ├── 任务描述
│   ├── 所属项目
│   ├── 优先级
│   ├── 标签
│   ├── 计划开始时间
│   ├── 截止时间
│   ├── 计划今日
│   └── 提醒设置
├── 个人记录
│   ├── 备注
│   ├── 附件
│   ├── 执行记录
│   └── 操作日志
└── 操作区
    ├── 编辑
    ├── 复制
    ├── 删除
    ├── 移动到项目
    └── 重新打开
```

### 9.6.3 详情页操作

| 操作         | 说明                   |
| ------------ | ---------------------- |
| 编辑         | 修改任务字段           |
| 状态切换     | 待办、进行中、已完成   |
| 标记完成     | 一键完成               |
| 重新打开     | 已完成任务重新恢复     |
| 设置优先级   | 高、中、低             |
| 修改截止时间 | 调整任务截止时间       |
| 标记计划今日 | 加入今日任务           |
| 添加标签     | 分类任务               |
| 添加备注     | 记录个人执行想法       |
| 上传附件     | 添加相关文件           |
| 复制任务     | 创建类似任务           |
| 移动项目     | 将任务移动到另一个项目 |
| 删除任务     | 删除到回收站           |

### 9.6.4 个人备注

个人版不需要团队评论，可以改为“个人备注”或“执行记录”。

用途：

1. 记录任务执行想法；
2. 记录参考资料链接；
3. 记录临时检查项；
4. 记录完成过程中的问题；
5. 记录复盘内容。

备注支持：

```text
文本
链接
简单列表
图片
```

---

## 9.7 任务列表视图

### 9.7.1 功能目标

用列表方式查看和管理任务，适合任务较多时批量处理。

### 9.7.2 列表字段

| 字段     | 说明                   |
| -------- | ---------------------- |
| 任务标题 | 可点击进入详情         |
| 状态     | 待办、进行中、已完成   |
| 优先级   | 高、中、低             |
| 截止时间 | 到期时间               |
| 计划今日 | 是否标记今天处理       |
| 标签     | 任务分类               |
| 所属项目 | 若为全部任务列表需展示 |
| 创建时间 | 任务创建时间           |
| 更新时间 | 最近更新时间           |

### 9.7.3 筛选条件

| 筛选项   | 说明                     |
| -------- | ------------------------ |
| 状态     | 待办、进行中、已完成     |
| 优先级   | 高、中、低               |
| 截止时间 | 今天、本周、本月、已逾期 |
| 标签     | 按标签筛选               |
| 计划今日 | 是否计划今天处理         |
| 所属项目 | 按项目筛选               |
| 关键词   | 搜索标题、描述、备注     |

### 9.7.4 排序规则

支持：

1. 按创建时间；
2. 按更新时间；
3. 按截止时间；
4. 按优先级；
5. 按手动排序。

默认排序：

```text
截止时间升序
```

---

## 9.8 今日任务

### 9.8.1 功能目标

帮助用户快速聚焦今天需要完成的任务。

### 9.8.2 页面结构

```text
今日任务页
├── 今日概览
│   ├── 今日到期
│   ├── 已逾期
│   ├── 计划今日
│   └── 已完成
├── 任务分组
│   ├── 已逾期
│   ├── 今天到期
│   ├── 计划今日
│   ├── 进行中
│   └── 已完成
└── 快速添加任务
```

### 9.8.3 今日任务来源

任务进入今日任务的条件：

1. 截止时间为今天；
2. 用户标记为“计划今日”；
3. 已逾期且未完成；
4. 用户手动置顶到今天；
5. 可配置：进行中的高优先级任务是否展示。

### 9.8.4 今日任务展示字段

| 字段     | 说明                 |
| -------- | -------------------- |
| 任务标题 | 主信息               |
| 所属项目 | 方便识别来源         |
| 优先级   | 高、中、低           |
| 截止时间 | 展示具体时间或日期   |
| 状态     | 待办、进行中、已完成 |
| 标签     | 可选                 |

### 9.8.5 今日任务操作

1. 勾选完成；
2. 进入详情；
3. 修改优先级；
4. 调整截止时间；
5. 移除“计划今日”；
6. 延期到明天；
7. 移到其他项目。

### 9.8.6 今日完成统计

顶部展示：

```text
今日应完成：8
已完成：3
未完成：5
完成率：37.5%
```

---

## 9.9 搜索功能

### 9.9.1 搜索范围

本期支持：

1. 任务标题；
2. 任务描述；
3. 任务备注；
4. 标签；
5. 项目名称。

后续可扩展：

1. 附件文件名；
2. 历史任务；
3. 完成记录。

### 9.9.2 搜索规则

1. 支持模糊搜索；
2. 支持关键词高亮；
3. 搜索结果按更新时间或相关性排序；
4. 搜索仅限用户自己的数据；
5. 无结果时展示空状态；
6. 支持按项目、状态、优先级筛选搜索结果。

---

## 9.10 标签管理

### 9.10.1 功能目标

帮助用户从项目之外对任务进行分类。

例如：

```text
工作
学习
生活
健康
副业
紧急
重要
等待中
外出办理
电脑端处理
```

### 9.10.2 标签字段

| 字段     | 说明       |
| -------- | ---------- |
| 标签名称 | 标签展示名 |
| 标签颜色 | 可选       |
| 创建时间 | 系统记录   |
| 状态     | 启用/停用  |

### 9.10.3 标签规则

1. 标签名称不能重复；
2. 标签可编辑；
3. 标签可删除；
4. 删除标签需二次确认；
5. 删除标签后，任务上的该标签移除；
6. 标签支持颜色区分；
7. 任务可添加多个标签；
8. 支持按标签筛选任务。

---

## 9.11 提醒功能

### 9.11.1 功能目标

帮助用户避免遗漏重要任务。

### 9.11.2 提醒类型

| 类型         | 说明                       |
| ------------ | -------------------------- |
| 截止前提醒   | 任务截止前提醒             |
| 截止当天提醒 | 截止日当天提醒             |
| 逾期提醒     | 任务逾期后提醒             |
| 每日待办提醒 | 每天早上提醒今日任务，可选 |
| 自定义提醒   | 用户指定提醒时间           |

### 9.11.3 提醒规则

1. 没有截止时间的任务不触发截止提醒；
2. 已完成任务不触发提醒；
3. 用户可关闭单个任务提醒；
4. 用户可全局关闭提醒；
5. 逾期任务可每天提醒一次，避免过度打扰；
6. 点击提醒进入任务详情；
7. 若任务已删除，提醒失效。

### 9.11.4 提醒渠道

根据产品端选择：

| 端       | 可选提醒方式         |
| -------- | -------------------- |
| Web      | 浏览器通知、站内提醒 |
| App      | Push 通知            |
| 小程序   | 订阅消息             |
| 本地应用 | 本地通知             |
| 日历集成 | 日历提醒             |

---

## 9.12 删除、归档与回收站

### 9.12.1 删除任务

1. 删除任务需二次确认；
2. 删除后进入回收站；
3. 回收站保留 30 天，可配置；
4. 超过保留期后自动清理；
5. 删除任务不影响项目本身；
6. 删除操作记录日志。

### 9.12.2 删除项目

删除项目时需要谨慎。

建议规则：

1. 删除项目需二次确认；
2. 弹窗提示项目内任务数量；
3. 项目删除后，项目内任务进入回收站；
4. 回收站内可恢复项目及其任务；
5. 超过保留期后自动清理。

提示文案示例：

```text
删除项目将同时删除该项目下的 23 个任务。删除后可在回收站中恢复，是否确认删除？
```

### 9.12.3 归档项目

归档项目适用于已完成或暂时不处理的项目。

规则：

1. 归档项目默认不在项目列表主视图展示；
2. 归档项目可在“已归档”中查看；
3. 归档项目可恢复；
4. 归档项目内的任务不删除；
5. 归档项目仍可搜索，视设置决定。

### 9.12.4 回收站

回收站展示：

| 字段         | 说明           |
| ------------ | -------------- |
| 名称         | 项目或任务名称 |
| 类型         | 项目/任务      |
| 删除时间     | 何时删除       |
| 自动清理时间 | 剩余保留时间   |
| 所属项目     | 若为任务需展示 |
| 操作         | 恢复、彻底删除 |

---

## 9.13 设置功能

### 9.13.1 基础设置

| 设置项       | 说明                 |
| ------------ | -------------------- |
| 默认视图     | 看板或列表           |
| 默认排序     | 手动、时间、优先级   |
| 主题         | 浅色、深色、跟随系统 |
| 每周开始日   | 周一或周日           |
| 一天开始时间 | 用于今日判断，可选   |

### 9.13.2 提醒设置

| 设置项       | 说明                    |
| ------------ | ----------------------- |
| 开启提醒     | 总开关                  |
| 截止前提醒   | 提前 1 天、当天、1 小时 |
| 逾期提醒     | 开启/关闭               |
| 每日待办提醒 | 开启/关闭               |
| 提醒时间     | 如每天 09:00            |

### 9.13.3 数据设置

| 设置项   | 说明                  |
| -------- | --------------------- |
| 数据备份 | 手动备份              |
| 自动备份 | 可选                  |
| 导入数据 | 后续支持              |
| 导出数据 | 可导出任务为 CSV/JSON |
| 清空数据 | 高危操作，需二次确认  |

---

# 10. 状态机设计

## 10.1 基础状态

个人版第一版建议使用最简单的三个状态：

```text
待办
进行中
已完成
```

## 10.2 状态定义

| 状态   | 含义                     |
| ------ | ------------------------ |
| 待办   | 任务已创建，但还没有开始 |
| 进行中 | 用户正在处理             |
| 已完成 | 用户确认完成             |

## 10.3 状态流转

```text
待办 → 进行中
待办 → 已完成
进行中 → 已完成
进行中 → 待办
已完成 → 进行中
已完成 → 待办
```

## 10.4 完成逻辑

1. 用户点击“完成”后，状态变为“已完成”；
2. 系统记录完成时间；
3. 任务从今日未完成任务中移除；
4. 项目完成进度更新；
5. 已完成任务可重新打开；
6. 重新打开后清除完成时间。

## 10.5 可选扩展状态

后续可增加：

| 状态   | 说明         |
| ------ | ------------ |
| 已暂停 | 暂时搁置     |
| 已取消 | 不再执行     |
| 等待中 | 等待外部条件 |

但第一版不建议增加过多状态。

---

# 11. 信息结构

## 11.1 项目对象

```text
项目
├── 项目ID
├── 项目名称
├── 项目描述
├── 项目颜色
├── 项目图标
├── 项目状态
├── 计划开始时间
├── 计划结束时间
├── 是否置顶
├── 是否归档
├── 创建时间
├── 更新时间
└── 任务统计
```

## 11.2 任务对象

```text
任务卡
├── 任务ID
├── 任务标题
├── 任务描述
├── 所属项目
├── 状态
├── 优先级
├── 计划开始时间
├── 截止时间
├── 计划今日
├── 完成时间
├── 标签
├── 备注
├── 附件
├── 提醒设置
├── 排序字段
├── 创建时间
└── 更新时间
```

---

# 12. 数据字段设计

## 12.1 项目表

| 字段名          | 类型     | 是否必填 | 说明                   |
| --------------- | -------- | -------- | ---------------------- |
| id              | string   | 是       | 项目唯一ID             |
| name            | string   | 是       | 项目名称               |
| description     | text     | 否       | 项目描述               |
| color           | string   | 否       | 项目颜色               |
| icon            | string   | 否       | 项目图标               |
| status          | enum     | 是       | 进行中、已完成、已归档 |
| plan_start_date | date     | 否       | 计划开始时间           |
| plan_end_date   | date     | 否       | 计划结束时间           |
| pinned          | boolean  | 是       | 是否置顶               |
| archived        | boolean  | 是       | 是否归档               |
| deleted         | boolean  | 是       | 是否删除               |
| sort_order      | int      | 否       | 排序                   |
| created_at      | datetime | 是       | 创建时间               |
| updated_at      | datetime | 是       | 更新时间               |

## 12.2 任务表

| 字段名          | 类型     | 是否必填 | 说明                 |
| --------------- | -------- | -------- | -------------------- |
| id              | string   | 是       | 任务唯一ID           |
| project_id      | string   | 是       | 所属项目             |
| title           | string   | 是       | 任务标题             |
| description     | text     | 否       | 任务描述             |
| status          | enum     | 是       | 待办、进行中、已完成 |
| priority        | enum     | 是       | 高、中、低           |
| plan_start_time | datetime | 否       | 计划开始时间         |
| due_time        | datetime | 否       | 截止时间             |
| planned_today   | boolean  | 是       | 是否计划今日         |
| completed_time  | datetime | 否       | 完成时间             |
| tags            | array    | 否       | 标签                 |
| note            | text     | 否       | 个人备注             |
| attachments     | array    | 否       | 附件                 |
| reminder        | object   | 否       | 提醒配置             |
| sort_order      | int      | 否       | 排序                 |
| deleted         | boolean  | 是       | 是否删除             |
| created_at      | datetime | 是       | 创建时间             |
| updated_at      | datetime | 是       | 更新时间             |

## 12.3 标签表

| 字段名     | 类型     | 是否必填 | 说明      |
| ---------- | -------- | -------- | --------- |
| id         | string   | 是       | 标签ID    |
| name       | string   | 是       | 标签名称  |
| color      | string   | 否       | 标签颜色  |
| status     | enum     | 是       | 启用/停用 |
| created_at | datetime | 是       | 创建时间  |
| updated_at | datetime | 是       | 更新时间  |

## 12.4 附件表

| 字段名     | 类型     | 是否必填 | 说明     |
| ---------- | -------- | -------- | -------- |
| id         | string   | 是       | 附件ID   |
| task_id    | string   | 是       | 任务ID   |
| file_name  | string   | 是       | 文件名   |
| file_type  | string   | 是       | 文件类型 |
| file_size  | int      | 是       | 文件大小 |
| file_url   | string   | 是       | 文件地址 |
| created_at | datetime | 是       | 上传时间 |
| deleted    | boolean  | 是       | 是否删除 |

## 12.5 提醒表

| 字段名      | 类型     | 是否必填 | 说明                           |
| ----------- | -------- | -------- | ------------------------------ |
| id          | string   | 是       | 提醒ID                         |
| task_id     | string   | 是       | 任务ID                         |
| remind_time | datetime | 是       | 提醒时间                       |
| remind_type | enum     | 是       | 截止前、截止当天、逾期、自定义 |
| status      | enum     | 是       | 待触发、已触发、已取消         |
| created_at  | datetime | 是       | 创建时间                       |

---

# 13. 页面流程

## 13.1 创建项目流程

```text
用户点击新建项目
  → 填写项目名称
  → 填写描述、颜色、计划时间
  → 系统校验
  → 创建成功
  → 用户进入项目
  → 开始创建任务
```

## 13.2 创建任务流程

```text
用户进入项目
  → 点击新建任务
  → 填写任务标题
  → 设置优先级、截止时间、标签
  → 标记是否计划今日
  → 保存
  → 任务进入看板
```

## 13.3 执行任务流程

```text
用户打开今日任务
  → 查看今天需要处理的任务
  → 进入任务详情
  → 将状态改为进行中
  → 执行任务
  → 添加备注或附件
  → 标记完成
```

## 13.4 项目复盘流程

```text
用户进入项目详情
  → 查看项目统计
  → 查看已完成任务
  → 查看逾期任务
  → 调整未完成任务
  → 更新后续计划
```

## 13.5 删除项目流程

```text
用户点击删除项目
  → 系统提示项目内任务数量
  → 用户二次确认
  → 项目和任务进入回收站
  → 用户可在回收站恢复
```

---

# 14. 非功能性需求

## 14.1 性能要求

| 场景         | 要求                |
| ------------ | ------------------- |
| 项目列表加载 | 首屏 2 秒内         |
| 看板加载     | 首屏 2 秒内         |
| 创建任务     | 响应 500ms 内       |
| 拖拽状态更新 | 即时反馈            |
| 搜索任务     | 常规数据量下 1 秒内 |
| 今日任务加载 | 1 秒内              |

## 14.2 数据安全

1. 所有数据仅用户本人可见；
2. 接口需要登录态校验；
3. 删除操作需二次确认；
4. 回收站保留一定时间；
5. 导出数据需用户主动确认；
6. 清空数据需强提示；
7. 附件访问需鉴权；
8. 支持本地或云端备份。

## 14.3 可用性要求

1. 空状态提供引导；
2. 操作失败有提示；
3. 拖拽失败可回滚；
4. 长文本自动截断；
5. 重要操作可撤销；
6. 支持键盘快捷键，可选；
7. 移动端需支持长按拖拽；
8. 任务卡片信息清晰，不拥挤。

## 14.4 隐私要求

1. 不展示他人数据；
2. 不支持公开分享，除非后续版本明确增加；
3. 用户可导出自己的数据；
4. 用户可删除自己的数据；
5. 若支持云同步，需说明同步范围。

---

# 15. 异常与边界情况

| 场景                 | 处理方式                         |
| -------------------- | -------------------------------- |
| 项目名称为空         | 不允许保存，提示“请输入项目名称” |
| 任务标题为空         | 不允许保存，提示“请输入任务标题” |
| 开始时间晚于结束时间 | 不允许保存                       |
| 截止时间早于当前时间 | 提示，但允许保存                 |
| 项目删除后查看       | 提示“项目不存在或已删除”         |
| 任务删除后查看       | 提示“任务不存在或已删除”         |
| 拖拽失败             | 回滚并提示                       |
| 附件过大             | 提示超过大小限制                 |
| 标签重复             | 不允许创建重复标签               |
| 今日任务为空         | 展示“今天暂无任务，去创建一个吧” |
| 项目无任务           | 展示空状态和“新建任务”按钮       |
| 网络异常             | 提示网络异常，支持重试           |
| 本地数据冲突         | 若支持多端同步，需提示冲突处理   |

---

# 16. 验收标准

## 16.1 项目功能验收

1. 用户可以创建项目；
2. 用户可以编辑项目名称、描述、颜色和状态；
3. 项目列表可按状态查看；
4. 项目卡片能展示任务统计和完成进度；
5. 用户可以归档项目；
6. 用户可以删除项目；
7. 删除项目后，项目内任务进入回收站；
8. 用户可以在回收站恢复项目。

## 16.2 任务功能验收

1. 用户可以创建任务；
2. 任务必须属于某个项目；
3. 用户可以编辑任务标题、描述、优先级、截止时间、标签；
4. 用户可以删除任务；
5. 任务删除后进入回收站；
6. 用户可以复制任务；
7. 用户可以将任务移动到其他项目；
8. 用户可以标记任务完成；
9. 用户可以重新打开已完成任务；
10. 任务完成时间记录正确。

## 16.3 看板验收

1. 看板默认展示待办、进行中、已完成；
2. 用户可以拖拽任务卡改变状态；
3. 拖拽后状态即时更新；
4. 拖拽失败可回滚；
5. 看板支持新建任务；
6. 看板支持筛选和搜索；
7. 逾期任务有明确视觉标识；
8. 已完成任务展示在已完成列。

## 16.4 今日任务验收

1. 今日到期任务能正确展示；
2. 计划今日任务能正确展示；
3. 已逾期任务能正确展示；
4. 完成任务后今日统计更新；
5. 用户可快速添加今日任务；
6. 用户可将任务延期到明天；
7. 用户可取消计划今日。

## 16.5 提醒验收

1. 设置截止提醒后能按时提醒；
2. 已完成任务不提醒；
3. 已删除任务不提醒；
4. 用户可关闭单个任务提醒；
5. 用户可全局关闭提醒；
6. 点击提醒可进入任务详情。

## 16.6 数据验收

1. 项目创建时间记录正确；
2. 任务创建时间记录正确；
3. 任务完成时间记录正确；
4. 项目进度计算正确；
5. 逾期任务判断正确；
6. 删除数据进入回收站；
7. 恢复数据后状态正确。

---

# 17. 上线计划建议

## 17.1 第一阶段：个人版 MVP

目标：让用户能完成“项目创建 → 任务拆解 → 看板执行 → 今日完成”的闭环。

包含：

1. 项目列表；
2. 新建项目；
3. 项目详情；
4. 任务看板；
5. 任务列表；
6. 新建任务；
7. 任务详情；
8. 状态：待办、进行中、已完成；
9. 优先级；
10. 截止时间；
11. 标签；
12. 计划今日；
13. 今日任务；
14. 搜索；
15. 筛选；
16. 删除与回收站；
17. 基础提醒。

## 17.2 第二阶段：个人效率增强

包含：

1. 子任务；
2. 任务清单；
3. 日历视图；
4. 周视图；
5. 任务模板；
6. 重复任务；
7. 附件增强；
8. 备注记录增强；
9. 数据导出；
10. 深色模式。

## 17.3 第三阶段：个人项目管理增强

包含：

1. 项目统计图表；
2. 完成趋势；
3. 逾期分析；
4. 时间预估；
5. 个人复盘报告；
6. 目标管理；
7. 项目阶段；
8. 里程碑；
9. 本地备份与云同步；
10. 多端同步。

---

# 18. 风险与注意事项

| 风险       | 影响               | 应对方式               |
| ---------- | ------------------ | ---------------------- |
| 功能过重   | 个人用户使用压力大 | 第一版保持轻量         |
| 字段过多   | 创建任务门槛高     | 仅保留标题必填         |
| 状态过多   | 维护成本高         | 默认三个状态           |
| 提醒过多   | 打扰用户           | 提供提醒开关           |
| 项目过多   | 用户难以管理       | 支持归档、置顶、搜索   |
| 任务过多   | 看板加载慢         | 分页、懒加载、虚拟列表 |
| 用户坚持难 | 工具使用频率低     | 强化今日任务和快捷创建 |
| 数据丢失   | 用户信任下降       | 提供回收站和备份能力   |

---

# 19. 推荐最终 MVP 方案

如果是个人使用，我建议第一版不要做复杂，直接采用下面这个方案：

## 19.1 产品形态

```text
项目 + 任务卡 + 看板 + 今日任务 + 提醒 + 回收站
```

## 19.2 核心页面

```text
项目列表
项目详情
任务看板
任务列表
今日任务
任务详情
回收站
设置
```

## 19.3 核心对象

```text
项目
任务卡
标签
今日任务
回收站
```

## 19.4 核心状态

```text
待办
进行中
已完成
```

## 19.5 核心字段

```text
任务标题
所属项目
状态
优先级
截止时间
计划今日
标签
备注
```

## 19.6 核心能力

```text
创建项目
创建任务
看板拖拽
任务完成
今日任务
截止提醒
搜索筛选
删除回收
```

## 19.7 暂不实现

```text
多人协作
任务指派
评论
@提醒
验收流程
权限管理
甘特图
任务依赖
工时统计
复杂自定义字段
```

---

# 20. 待确认问题

如果后续要继续细化，建议确认以下问题：

1. 需要日历视图
2. 需要子任务
3. 需要重复任务
4. 需要附件
5. 提醒通过什么方式触达：站内通知
6. 需要“项目阶段”或“里程碑”概念

