# 个人项目任务卡产品需求文档-PRD（TimeWrite 模块版）

| 项目名称 | 个人项目任务卡（TimeWrite 内置模块）                 |
| -------- | ---------------------------------------------------- |
| 文档版本 | V1.1                                                 |
| 调整日期 | 2026-09-03                                           |
| 产品定位 | TimeWrite 内置的"项目 → 任务卡 → 看板 → 今日任务"个人管理模块 |
| 使用对象 | 仅当前用户本人                                       |
| 核心场景 | 管理自己的项目、拆解任务、看板执行、今日聚焦、到期提醒、复盘 |

---

## 1. 文档说明与决策记录

### 1.1 调整历史

本方案历经两次调整：

1. 由"团队任务卡"调整为"个人项目任务卡"（V1.0）；
2. 由"独立产品/多端通用"调整为 **"TimeWrite 内置模块（Tauri + SQLite 本地单机）"**，并收敛 MVP 范围（V1.1）。

### 1.2 关键决策记录（V1.1）

| 决策点 | 结论 | 说明 |
| ------ | -------------------------------------------- | ------------------------------------------------------------ |
| 产品形态 | TimeWrite 内置模块 | 本地 SQLite 存储，无登录、无服务端、无云同步；与日记、英语字典、世界观资料库同为应用内功能 |
| 与现有"个人日程"关系 | 任务卡模块取代日程 | 提供一次性数据迁移，旧日程 UI 保留一个版本后移除 |
| MVP 范围 | P0 + P1 | P0 完成使用闭环；P1 补齐搜索筛选、标签、截止提醒、列表视图（详见第 6 节） |
| 提醒触达 | 站内提醒 + 本地系统通知 | 应用运行时定时轮询 + 启动时补扫；不引入 Push / 订阅消息 / 日历集成 |

### 1.3 与团队版的主要差异（保留）

| 模块       | 团队版                         | 个人版（TimeWrite 模块） |
| ---------- | ------------------------------ | ------------------------ |
| 使用对象   | 多人协作                       | 仅用户本人               |
| 用户角色   | 管理员、负责人、参与人、查看者 | 无角色区分               |
| 任务负责人 | 可指派他人                     | 默认且仅为用户本人       |
| 权限体系   | 复杂权限                       | 不需要，本人全部权限     |
| 评论协作   | 评论、@、参与人                | 不需要，改为个人备注     |
| 验收流程   | 提交验收、驳回                 | 不需要                   |
| 存储与网络 | 服务端、登录态、云同步         | 本地 SQLite，无网络依赖  |
| 核心价值   | 团队协作透明化                 | 个人工作规划、执行和复盘 |

### 1.4 与 TimeWrite 现有模块的关系

| 现有能力 | 与本模块的关系 | 处理方式 |
| -------- | -------------- | -------- |
| 个人日程（schedules：日记面板日程清单、日历圆点） | **被取代** | V1.1 起新数据写入任务模块；提供迁移入口；旧 UI 保留至下一版本后下线（见 6.4） |
| 回收站（作品/卷/章节软删除 + 恢复） | 复用 | 项目/任务沿用同一套软删除模式与交互 |
| 本地备份（AES-256-GCM 全量导出/恢复） | 复用 | 数据自动纳入备份范围，不单独设计 |
| TipTap 富文本编辑器 | 复用 | 任务描述用 TipTap（图片/列表等能力顺带获得） |
| home-header 扩展点 / 独立悬浮窗口（如英语字典） | 复用 | 本模块入口走同一模式（见 8.1） |
| 主题体系（HSL CSS 变量） | 复用 | 不重复做主题设置 |

---

## 2. 产品定位

```text
TimeWrite 内置的一个面向个人用户的项目管理与任务卡工具模块。
```

帮助用户：

1. 管理自己的项目；
2. 将项目拆解为任务卡；
3. 用看板方式可视化任务状态；
4. 跟踪任务优先级和截止时间；
5. 记录个人执行过程（备注）；
6. 查看今日待办和逾期任务；
7. 定期复盘项目进度和个人完成情况。

---

## 3. 产品目标

### 3.1 用户目标

用户希望通过这个模块解决：

1. 自己的项目和任务太多，容易忘记；
2. 任务散落在笔记、聊天、脑子里，缺少统一管理；
3. 不知道当前项目进展到哪一步；
4. 不知道今天应该优先做什么；
5. 任务缺少截止时间，容易拖延；
6. 完成后缺少记录，无法复盘。

### 3.2 产品目标

1. 快速创建项目和任务；
2. 用看板清晰看到任务状态；
3. 明确每个任务的优先级和截止时间；
4. 快速找到今天要做的事；
5. 跟踪每个项目的整体进度；
6. 回顾已完成任务和项目成果（复盘）。

### 3.3 核心衡量指标

单机自用工具不采集外部产品指标，以下数据作为**本地统计与自我复盘依据**（不做上报，仅在模块内统计页/周报中展示）：

| 指标             | 说明                          |
| ---------------- | ----------------------------- |
| 创建项目数       | 用户是否开始用项目管理事务    |
| 创建任务数       | 用户是否持续记录任务          |
| 任务完成率       | 已完成任务 / 总任务           |
| 今日任务完成率   | 今日完成任务 / 今日应完成任务 |
| 逾期任务数       | 未按期完成任务数量            |
| 平均任务完成时长 | 从创建到完成的时间            |
| 每周/每月完成数  | 用于周报与复盘的自我驱动数据  |

---

## 4. 用户对象

### 4.1 用户定义

本模块仅面向单一用户本人：

```text
当前用户 = 项目创建者 = 任务创建者 = 任务执行者 = 任务管理者
```

### 4.2 不使用角色权限

不需要管理员、负责人、参与人、验收人、查看者等概念。所有数据本地存储，默认仅用户本人可见、可编辑、可删除。

---

## 5. 核心使用场景

### 5.1 场景一：创建自己的项目

创建"找工作准备、个人网站开发、学习计划、健身计划、副业项目、年度目标拆解"等项目，填写名称、描述、计划起止时间、颜色与状态。

### 5.2 场景二：把项目拆解成任务

进入项目后拆解任务卡，例如"个人网站开发"：

```text
梳理网站栏目 → 设计首页结构 → 准备文案内容 → 开发首页 → 部署上线 → 检查移动端适配
```

每个任务卡可设置优先级、截止时间、标签与备注。

### 5.3 场景三：用看板查看项目进度

默认三列（待办 / 进行中 / 已完成），通过拖拽更新任务状态。

### 5.4 场景四：查看今天需要做什么

"今日任务"展示：今天到期、已逾期未完成、标记"计划今日"、进行中的高优先级任务。

### 5.5 场景五：任务到期提醒

站内提醒 + 本地系统通知：截止前 1 天 / 截止当天 / 截止前 1 小时（可配置）、逾期提醒、每日待办提醒（可选）。

### 5.6 场景六：项目复盘

查看项目统计：总任务 / 已完成 / 进行中 / 逾期 / 完成进度 / 最近完成 / 长期未处理，并结合个人周报回顾。

---

## 6. 需求范围

### 6.1 P0 范围（本期 · 使用闭环必需）

1. 项目管理：新建、列表、详情、编辑、归档；
2. 任务卡：创建、编辑、详情、查看；
3. 任务状态：待办 / 进行中 / 已完成；
4. 看板视图：拖拽改状态、列内排序、卡片信息展示；
5. 任务完成 / 重新打开，完成时间记录；
6. 计划今日（标记今天处理）与今日任务页；
7. 项目进度统计；
8. 删除与回收站（项目/任务软删除、恢复、彻底删除）；
9. 逾期任务的视觉标识。

### 6.2 P1 范围（本期 · 体验增强）

1. 搜索（标题 / 描述 / 备注 / 标签 / 项目名）；
2. 筛选（状态 / 优先级 / 标签 / 截止时间 / 是否逾期 / 所属项目）；
3. 标签管理（新建、编辑、删除、颜色、按标签筛选）；
4. 截止提醒：截止前提醒、截止当天提醒、逾期提醒、每日待办提醒（可选），站内 + 本地系统通知；
5. 任务列表视图（与看板可切换）；
6. 复制任务、任务移动到其他项目；
7. 项目置顶。

### 6.3 P2（暂不包含，放入后续版本）

1. 子任务 / 任务清单；
2. 重复任务 / 任务模板；
3. 附件上传与管理（数据表预留，见 12.4）；
4. 备注富文本化（图片、链接卡片）；
5. 操作日志 / 执行记录时间线；
6. 回收站 30 天自动清理（先提供手动清空）；
7. 日历视图、周视图；
8. 项目统计图表、完成趋势、周报；
9. 项目阶段 / 里程碑 / 目标管理；
10. 复杂自定义字段、自动化规则、工时统计。

### 6.4 取代"个人日程"与数据迁移

**目标**：任务卡模块逐步取代现有"个人日程"（书库首页日记面板中的日程清单），消除"日程 vs 任务"双轨心智负担。

迁移策略（分两步，降低风险）：

1. **本版本（V1.1+）**：
   - 新数据一律写入任务模块；
   - 提供一次性迁移入口（设置 → 数据 → 迁移个人日程）：
     - 将现有未删除的日程按"日期分组"迁移到任务模块的一个默认项目（如"个人事务"，颜色取主题默认）；
     - 每条日程映射为任务：`title` 取日程标题，`due_time` 取日程日期（当天 23:59），已勾选完成的日程置为"已完成"并记录完成时间，`planned_today` 仅当日程日期为今天时置位；
     - 迁移前自动全量备份提示；迁移幂等（重复执行不产生重复任务）；
   - 旧"日程"入口保留，但入口旁提示"已由任务卡替代"，并默认隐藏"添加日程"按钮。
2. **下一版本**：确认迁移完成且用户无回退诉求后，移除 schedules 旧 UI 与数据表。

---

## 7. 核心概念设计

```text
项目
└── 任务卡
    ├── 状态（待办 / 进行中 / 已完成）
    ├── 优先级（高 / 中 / 低）
    ├── 时间（开始 / 截止 / 完成）
    ├── 计划今日
    ├── 标签
    └── 备注
```

### 7.1 项目

任务（及从日程迁移来的任务）的容器，有名称、描述、颜色、状态（进行中/已完成/已归档）、计划起止日期。

### 7.2 任务卡

具体要做的事情，必属于某个项目。示例：整理作品集内容、写简历初稿、背 30 个单词、完成首页设计。

### 7.3 看板

展示任务状态的视图，默认三列：待办 / 进行中 / 已完成。

### 7.4 今日任务

个人执行视图，回答三个问题：

```text
我今天应该做什么？哪些任务已逾期？哪些今天必须完成？
```

**来源规则（P0）**：截止时间为今天 / 标记"计划今日" / 已逾期未完成 / 进行中的高优先级任务（默认展示，可在设置中关闭）。

**"计划今日"滚动规则（防脏数据）**：

```text
每天自然日切换时（本地 00:00 后首次启动或模块激活时）自动清理：
- 昨日标记"计划今日"且仍未完成的任务 → 自动取消标记，保留在原项目待办中；
- 用户可在今日任务页手动将某任务"顺延到今天"（重新标记计划今日或改截止时间到今天）。
```

---

## 8. 产品架构

### 8.1 入口与窗口形态

仿照英语字典·生词本模式（TimeWrite 插件系统 home-header 扩展点 + 独立悬浮窗口）：

```text
入口 1：书库首页顶部"任务卡"图标（home-header 扩展点，角标显示今日到期+逾期数）
入口 2：全局命令面板（command palette）
入口 3：设置 → 数据 → "迁移个人日程"入口（6.4）
窗口形态：独立悬浮窗口（可停靠主窗口右侧），复用英语字典窗口的基础设施（位置记忆、缩放）
```

### 8.2 模块结构

```text
任务卡模块（独立窗口）
├── 首页/今日任务
│   ├── 今日概览（今日应完成 / 已完成 / 完成率）
│   ├── 分组列表（已逾期 / 今天到期 / 计划今日 / 进行中 / 已完成）
│   └── 快速添加任务
├── 项目列表
│   ├── 全部 / 进行中 / 已完成 / 已归档
│   └── 新建项目
├── 项目详情
│   ├── 项目头部（名称/状态/描述/统计/编辑/新建任务）
│   ├── 视图切换（看板 / 列表）
│   ├── 筛选与搜索
│   └── 任务区域
├── 任务详情（侧滑/弹层）
│   ├── 标题与状态
│   ├── 基础信息（项目/优先级/标签/时间/计划今日/提醒）
│   ├── 个人备注
│   └── 操作（编辑/复制/移动到项目/删除/完成）
├── 搜索页
├── 回收站
└── 设置（提醒偏好 / 标签管理 / 数据迁移与备份）
```

---

## 9. 功能需求详述

> 每节标注 P0 / P1 / P2，P0、P1 为本期范围。

### 9.1 项目列表（P0）

#### 9.1.1 功能目标

查看和管理所有项目。

#### 9.1.2 页面结构

```text
项目列表
├── 顶部：搜索项目 / 新建项目
├── 状态页签：全部 / 进行中 / 已完成 / 已归档
└── 项目卡片
    ├── 名称、颜色、描述（1-2 行截断）
    ├── 任务统计（总/已完成/进行中/逾期）
    ├── 完成进度条
    ├── 最近更新时间
    └── 状态角标
```

#### 9.1.3 项目排序（P1 手动排序可选）

```text
默认：置顶在前 → 进行中在前 → 最近更新时间倒序
```

### 9.2 新建项目（P0）

#### 9.2.1 项目字段

| 字段         | 类型     | 必填 | 说明                 |
| ------------ | -------- | ---- | -------------------- |
| 项目名称     | 文本     | 是   | 最多 50 字           |
| 项目描述     | 文本     | 否   | 目标、背景说明       |
| 项目颜色     | 颜色选择 | 否   | 默认系统分配         |
| 项目图标     | Emoji    | 否   | 可选                 |
| 项目状态     | 单选     | 否   | 默认"进行中"         |
| 计划开始日期 | 日期     | 否   | 可选                 |
| 计划结束日期 | 日期     | 否   | 可选，不能早于开始日 |

#### 9.2.2 校验与创建后行为

1. 名称非空且不超长；
2. 开始日期不得晚于结束日期；
3. 创建成功提示，可选"继续创建 / 进入项目"；
4. 系统记录创建时间与更新时间。

### 9.3 项目详情（P0）

#### 9.3.1 页面结构

```text
项目详情
├── 项目头部（返回 / 名称 / 状态 / 描述 / 统计 / 编辑 / 新建任务）
├── 视图切换：看板视图（默认） / 列表视图（P1）
├── 筛选区：状态 / 优先级 / 标签 / 截止时间 / 是否逾期
└── 任务区域
```

#### 9.3.2 项目统计

顶部轻量统计：总任务 / 待办 / 进行中 / 已完成 / 逾期 / 完成进度（实时计算）。

### 9.4 任务看板（P0）

#### 9.4.1 默认列

| 状态列 | 说明                   |
| ------ | ---------------------- |
| 待办   | 已创建，还没开始       |
| 进行中 | 正在处理               |
| 已完成 | 已经完成               |

第一版固定三列，不做自定义列与"已暂停/已取消"（P2）。

#### 9.4.2 任务卡片展示

| 字段     | 展示 | 说明               |
| -------- | ---- | ------------------ |
| 任务标题 | 是   | 主信息             |
| 优先级   | 是   | 高/中/低，图标区分 |
| 截止时间 | 是   | 逾期红色高亮       |
| 标签     | 是   | 最多展示 2-3 个     |

#### 9.4.3 看板操作

| 操作     | 说明                       |
| -------- | -------------------------- |
| 新建任务 | 当前列底部快速新建         |
| 点击卡片 | 打开任务详情（弹层）       |
| 拖拽卡片 | 改状态（跨列）或排序（同列） |
| 快捷完成 | 卡片左侧勾选，完成并记录时间 |
| 筛选/搜索 | 项目内按优先级/标签/时间筛选、关键词搜索 |

#### 9.4.4 拖拽与排序规则（解决规则冲突）

1. 跨列拖拽 → 更新状态；拖到"已完成"记录完成时间，从"已完成"拖回则**清空**完成时间；
2. 同列拖拽 → 手动排序；
3. **排序策略互斥**：默认"手动排序"（拖拽后不被打乱）；用户可选"按截止时间 / 按优先级 / 按创建时间 / 按更新时间"，选择规则排序时禁用同列手动拖拽；
4. 无论哪种排序，**逾期任务均以红色角标强调，不做强制置顶**（避免与手动顺序冲突）；
5. 拖拽失败需提示并回滚（本地数据库事务）。

### 9.5 新建任务卡（P0）

#### 9.5.1 入口

项目详情"新建任务" / 看板列底部"添加任务" / 今日任务页快速添加 / 全局命令面板。

#### 9.5.2 任务字段

| 字段         | 类型       | 必填 | 默认值   | 说明                     |
| ------------ | ---------- | ---- | -------- | ------------------------ |
| 任务标题     | 文本       | 是   | 空       | 最多 100 字              |
| 任务描述     | TipTap 富文本 | 否 | 空       | 记录细节；MVP 编辑能力从简 |
| 所属项目     | 单选       | 是   | 当前项目 | 必属于某项目             |
| 状态         | 单选       | 否   | 待办     | -                        |
| 优先级       | 单选       | 是   | 中       | 高/中/低                 |
| 计划开始时间 | 日期时间   | 否   | 空       | 可选                     |
| 截止时间     | 日期时间   | 否   | 空       | 建议填写；可配置为必填   |
| 计划今日     | 开关       | 否   | 否       | 标记今天处理             |
| 标签         | 多选       | 否   | 空       | 从已有标签中选，可即时新建 |
| 备注         | 纯文本     | 否   | 空       | 个人执行记录（图片/富文本 P2） |
| 提醒         | 开关/时间  | 否   | 关       | 见 9.11                   |

#### 9.5.3 校验

1. 标题非空、不超长；所属项目非空；优先级非空；
2. 开始时间不得晚于截止时间；
3. 截止时间早于当前 → 提示但允许保存（作为逾期任务）；
4. 删除进回收站（软删除）。

### 9.6 任务详情（P0，弹层形式）

#### 9.6.1 结构

```text
任务详情
├── 顶部：关闭 / 标题（内联编辑）/ 状态切换 / 完成按钮 / 更多操作
├── 基础信息：描述 / 所属项目 / 优先级 / 标签 / 开始时间 / 截止时间 / 计划今日 / 提醒
├── 个人备注：纯文本（P0）；富文本/图片（P2）
└── 操作：编辑 / 复制（P1）/ 移动到项目（P1）/ 删除
```

#### 9.6.2 详情操作（标 P0/P1）

| 操作             | 优先级 | 说明                   |
| ---------------- | ------ | ---------------------- |
| 编辑字段         | P0     | 修改任务字段           |
| 状态切换 / 标记完成 | P0   | 三态切换，完成记录时间 |
| 重新打开         | P0     | 恢复并清空完成时间     |
| 修改截止时间     | P0     | 更新后续提醒           |
| 标记/取消计划今日 | P0    | -                      |
| 添加/移除标签    | P0     | 标签即时管理           |
| 个人备注         | P0     | 纯文本                 |
| 复制任务         | P1     | 复制标题/描述/标签，状态置待办 |
| 移动到项目       | P1     | 目标项目选择器         |

### 9.7 任务列表视图（P1）

#### 9.7.1 字段与操作

表格列：任务标题 / 状态 / 优先级 / 截止时间 / 计划今日 / 标签 / 所属项目（全局列表时）/ 创建时间。支持行内勾选完成、批量操作（批量完成/删除，P2 可扩展）。

#### 9.7.2 筛选与排序

筛选：状态 / 优先级 / 截止时间（今天/本周/本月/已逾期）/ 标签 / 计划今日 / 所属项目 / 关键词。
排序：按创建/更新时间、截止时间、优先级；**默认按截止时间升序**；支持"规则排序"与手动排序互斥（同 9.4.4）。

### 9.8 今日任务（P0）

#### 9.8.1 页面结构

```text
今日任务
├── 今日概览：今日应完成 n · 已完成 m · 完成率 x%
├── 分组列表：已逾期 / 今天到期 / 计划今日 / 进行中 / 已完成
└── 快速添加任务（默认进入当前最活跃项目，可选项目）
```

#### 9.8.2 操作

勾选完成 / 打开详情 / 修改优先级 / 调整截止时间 / 取消计划今日 / **顺延到明天**（P1：改截止时间为明天并保留计划今日）/ 移到其他项目（P1）。

#### 9.8.3 完成联动

完成任务 → 状态变更、记录完成时间、从今日未完成组移除、项目进度即时更新、相关提醒取消。

### 9.9 搜索与筛选（P1）

1. 搜索范围：任务标题 / 描述 / 备注 / 标签 / 项目名称；模糊搜索 + 关键词高亮；
2. 结果支持按项目 / 状态 / 优先级 / 是否逾期二次筛选；
3. 全局搜索入口（命令面板 / 搜索页）；项目内搜索作用于当前项目；
4. 无结果空状态引导"新建任务"。

### 9.10 标签管理（P1）

1. 标签字段：名称（唯一，不超长）、颜色、启用/停用；
2. 编辑、删除（二次确认；删除后从所有任务移除该标签，需在任务表 `task_tags` 级联处理）；
3. 任务可加多个标签；按标签筛选；
4. 推荐示例标签：工作 / 学习 / 生活 / 健康 / 副业 / 紧急 / 等待中 / 外出办理 / 电脑端处理。

### 9.11 提醒（P1）

#### 9.11.1 提醒类型

| 类型       | 说明                                             |
| ---------- | ------------------------------------------------ |
| 截止前提醒 | 截止前 1 天 / 当天 / 前 1 小时（用户配置，可多选或自定提前量） |
| 截止当天提醒 | 截止日当天到达时提醒                           |
| 逾期提醒   | 逾期后每天一次，避免过度打扰                     |
| 每日待办提醒 | 每天早上提醒今日任务，默认关闭，可配置时间（如 09:00） |

#### 9.11.2 规则

1. 无截止时间任务不触发截止类提醒；已完成/已删除任务不触发；
2. 单任务可关闭提醒；设置页可全局关闭；
3. 提醒修改/截止时间变更后，重新计算提醒点。

#### 9.11.3 触达与触发机制（纯本地）

```text
触达：站内提醒中心（模块内"铃铛"列表，未读红点）+ 系统本地通知
（tauri-plugin-notification，点击通知定位到任务详情）

触发：
- 应用运行时：前端/后端定时器每分钟扫描一次待触发提醒；
- 应用冷启动：启动时扫描补齐错过的到期提醒（标记"已触发"并去重，不重复轰炸）。
```

不需要提醒调度表与定时任务常驻进程：提醒状态存任务字段（见 12.2 `remind_at`），每次扫描为"计算式触发"。

### 9.12 删除、归档与回收站（P0）

#### 9.12.1 删除规则

| 对象 | 规则 |
| ---- | ---- |
| 删除任务 | 二次确认 → 软删除进回收站；不影响项目 |
| 删除项目 | 二次确认并提示任务数量（"将同时删除该项目下 n 个任务，可在回收站恢复"）→ 项目及任务一并进回收站 |
| 归档项目 | 从主列表隐藏，可在"已归档"页签查看与恢复；归档不删除任务 |
| 回收站 | 展示名称/类型/删除时间/所属项目；支持恢复（任务恢复回原项目，项目恢复连同其任务）与彻底删除（二次确认） |

1. 恢复后状态正确（项目/任务回到删除前状态与所属关系）；
2. 自动清理（保留 30 天）为 P2，本期先提供"手动清空回收站"；
3. 删除与恢复均记录到任务更新（`updated_at`）。

### 9.13 设置

| 设置项 | 范围 | 说明 |
| ------ | ---- | ---- |
| 默认视图（看板/列表）、默认排序 | P2 前给固定默认值 | 本期不暴露（减少设置项） |
| 提醒偏好（截止前提前量、每日待办时间、总开关） | P1 | 见 9.11 |
| 标签管理 | P1 | 见 9.10 |
| 主题 | - | 跟随 TimeWrite 全局主题，不单独设置 |
| 迁移个人日程 | P0 | 见 6.4 |
| 数据备份/导出 | P2 | 纳入 TimeWrite 全量备份；导出 CSV/JSON 后续 |
| 清空数据 | P2 | 高危操作需二次确认 + 输入确认 |

---

## 10. 状态机设计

### 10.1 基础状态与流转

```text
待办 ⇄ 进行中 → 已完成（可 ⇄ 重新打开回 待办/进行中）
待办 → 已完成
```

### 10.2 完成逻辑

1. 点击完成 / 拖入已完成列 / 卡片勾选 → 状态置"已完成"，记录 `completed_time`；
2. 从今日未完成任务移除；项目进度即时更新；
3. 重新打开 → 状态回 待办/进行中，**清空完成时间**；
4. 取消相关待触发提醒。

### 10.3 可选扩展（P2）

已暂停 / 已取消 / 等待中 —— 本期不引入。

---

## 11. 信息结构

### 11.1 项目对象

```text
项目
├── 项目ID ├── 名称 ├── 描述 ├── 颜色 ├── 图标(Emoji)
├── 状态（进行中/已完成/已归档）├── 计划开始日期 ├── 计划结束日期
├── 是否置顶 ├── 是否删除 ├── 删除时间
├── 创建时间 ├── 更新时间 └── 任务统计（实时聚合，不落库）
```

### 11.2 任务对象

```text
任务卡
├── 任务ID ├── 所属项目ID ├── 标题 ├── 描述(TipTap HTML)
├── 状态 ├── 优先级 ├── 计划开始时间 ├── 截止时间
├── 计划今日 ├── 完成时间 ├── 备注
├── 提醒时间(remind_at) ├── 提醒类型
├── 标签（通过 task_tags 关联）├── 排序
├── 是否删除 ├── 删除时间 ├── 创建时间 └── 更新时间
```

---

## 12. 数据字段设计

存储：`time_write.db`（与现有数据同库），新增 `projects`、`tasks`、`tags`、`task_tags`、`attachments`（P2 预留）表。遵循现有 repository → service → command 分层与软删除模式。

### 12.1 项目表 projects

| 字段名          | 类型     | 必填 | 说明                          |
| --------------- | -------- | ---- | ----------------------------- |
| id              | string   | 是   | 项目唯一ID                    |
| name            | string   | 是   | 项目名称                      |
| description     | text     | 否   | 项目描述                      |
| color           | string   | 否   | 颜色（主题色值）              |
| icon            | string   | 否   | Emoji 图标                    |
| status          | enum     | 是   | active / completed / archived |
| plan_start_date | date     | 否   | 计划开始日期                  |
| plan_end_date   | date     | 否   | 计划结束日期                  |
| pinned          | boolean  | 是   | 是否置顶（P1）                |
| deleted         | boolean  | 是   | 软删除                        |
| deleted_at      | datetime | 否   | 删除时间（回收站清理依据）    |
| sort_order      | int      | 否   | 排序（P1 手动排序用）         |
| created_at      | datetime | 是   | 创建时间                      |
| updated_at      | datetime | 是   | 更新时间                      |

> 修正：删除冗余的 `archived` 布尔，归档即 `status=archived`；补充 `deleted_at` 支撑回收站保留期。

### 12.2 任务表 tasks

| 字段名          | 类型     | 必填 | 说明                               |
| --------------- | -------- | ---- | ---------------------------------- |
| id              | string   | 是   | 任务唯一ID                         |
| project_id      | string   | 是   | 所属项目                           |
| title           | string   | 是   | 标题                               |
| description     | text     | 否   | 描述（TipTap HTML）                |
| status          | enum     | 是   | todo / doing / done                |
| priority        | enum     | 是   | high / medium / low                |
| plan_start_time | datetime | 否   | 计划开始时间                       |
| due_time        | datetime | 否   | 截止时间                           |
| planned_today   | boolean  | 是   | 计划今日（按 7.4 规则每日清理）    |
| completed_time  | datetime | 否   | 完成时间（重开时清空）             |
| note            | text     | 否   | 个人备注（纯文本）                 |
| remind_at       | datetime | 否   | 下次提醒时间（触发后推进/清除）    |
| remind_type     | enum     | 否   | 截止前 / 截止当天 / 逾期 / 每日待办 |
| sort_order      | int      | 否   | 列内手动排序                       |
| deleted         | boolean  | 是   | 软删除                             |
| deleted_at      | datetime | 否   | 删除时间                           |
| created_at      | datetime | 是   | 创建时间                           |
| updated_at      | datetime | 是   | 更新时间                           |

> 修正：`tags` 由 array 拆出为关联表（12.3 task_tags）；`attachments` 独立表（12.4，P2）；`reminder` 对象字段简化为 `remind_at + remind_type`（单提醒点，多提醒/复杂调度 P2 再拆表）。

### 12.3 标签表 tags 与关联表 task_tags

tags：

| 字段名     | 类型     | 必填 | 说明      |
| ---------- | -------- | ---- | --------- |
| id         | string   | 是   | 标签ID    |
| name       | string   | 是   | 名称（唯一） |
| color      | string   | 否   | 颜色      |
| status     | enum     | 是   | enabled / disabled |
| created_at | datetime | 是   | 创建时间  |
| updated_at | datetime | 是   | 更新时间  |

task_tags：

| 字段名     | 类型   | 必填 | 说明                 |
| ---------- | ------ | ---- | -------------------- |
| task_id    | string | 是   | 任务ID（联合主键）   |
| tag_id     | string | 是   | 标签ID（联合主键）   |
| created_at | datetime | 是 | 关联时间             |

> 好处：删除标签可级联清理；按标签统计/筛选走 SQL 索引，不做全表 JSON 扫描。

### 12.4 附件表 attachments（P2 预留）

| 字段名     | 类型     | 必填 | 说明                                 |
| ---------- | -------- | ---- | ------------------------------------ |
| id         | string   | 是   | 附件ID                               |
| task_id    | string   | 是   | 任务ID                               |
| file_name  | string   | 是   | 原始文件名（展示用）                 |
| file_type  | string   | 是   | 文件类型                             |
| file_size  | int      | 是   | 文件大小（字节）                     |
| local_path | string   | 是   | 应用数据目录内相对路径（非网络 URL） |
| created_at | datetime | 是   | 上传时间                             |
| deleted    | boolean  | 是   | 软删除（彻底删除时同步清理文件）     |

> 修正：去掉 `file_url`/鉴权语义（本地单机）；文件实体统一存放 `应用数据目录/attachments/projects/`，随任务恢复/彻底删除联动。

### 12.5 schedules（个人日程）迁移说明

保留原 `schedules` 表结构不变，仅执行 6.4 的一次性读取迁移；旧表在下一版本随旧 UI 下线后删除（先导出备份再由用户确认清理）。

---

## 13. 页面流程

### 13.1 创建项目 → 创建任务（P0 主路径）

```text
入口(任务卡图标/命令面板) → 项目列表 → 新建项目 → 填名称等 → 创建成功
  → 进入项目 → 新建任务 → 填标题/优先级/截止/标签 → 保存 → 进入看板对应列
```

### 13.2 执行任务主路径

```text
今日任务 → 查看今日分组 → 打开任务详情 → 置为进行中 → 执行
  → 添加备注 → 标记完成（记录完成时间）→ 项目进度即时更新
```

### 13.3 删除与恢复

```text
删除任务/项目（二次确认）→ 回收站 → 恢复（回到原项目）/ 彻底删除（二次确认）
```

### 13.4 迁移个人日程（一次性）

```text
设置 → 迁移个人日程 → 备份提示 → 确认迁移 → 生成"个人事务"项目与对应任务
  → 结果报告（迁移 n 条，完成 m 条）→ 旧入口显示替代提示
```

---

## 14. 非功能性需求

### 14.1 性能要求（本地 SQLite，目标宽松即达）

| 场景         | 要求                |
| ------------ | ------------------- |
| 项目列表/看板加载 | 首屏 1 秒内      |
| 创建/编辑任务 | 500ms 内            |
| 拖拽状态更新 | 即时反馈（本地事务，失败回滚） |
| 搜索         | 常规数据量下 1 秒内 |
| 启动扫描补提醒 | 不阻塞界面          |

### 14.2 数据安全

1. 全部数据仅本机、本人可见；不联网、不上报；
2. 关键写操作（删除项目/任务、清空回收站、迁移）需二次确认；
3. 删除为软删除，支持回收站恢复；自动纳入 TimeWrite 全量备份（AES-256-GCM）；
4. 附件文件与数据库在备份/迁移时一致处理。

### 14.3 可用性要求

1. 空状态提供引导（"今天暂无任务，去创建一个吧"/"项目暂无任务"）；
2. 操作失败有提示；拖拽失败回滚；
3. 长文本截断；卡片信息清晰不拥挤；
4. 重要操作可撤销（软删除即撤销通道）；
5. 支持键盘快捷键：`N` 新建任务（聚焦时）、`Esc` 关闭弹层、命令面板全局唤起（二期可扩展）；
6. 逾期视觉：截止时间红色 + 卡片角标，不依赖颜色单通道（辅助功能）。

### 14.4 隐私

本地单机数据天然隐私；导出（P2）与备份均加密；不引入任何遥测。

---

## 15. 异常与边界情况

| 场景 | 处理方式 |
| ---- | -------- |
| 项目/任务名称为空、超长 | 不允许保存并提示 |
| 开始时间晚于截止时间 | 不允许保存并提示 |
| 截止时间早于当前 | 提示"已逾期"，允许保存 |
| 删除后访问项目/任务 | 提示"不存在或已删除"，引导回收站 |
| 拖拽失败 | 回滚并提示 |
| 标签重名 | 不允许创建，提示选择已有标签 |
| 今日任务为空 | 展示空状态引导 |
| 项目无任务 | 空状态 + 新建任务按钮 |
| 昨日"计划今日"未完成 | 自然日切换自动取消标记（7.4 滚动规则） |
| 提醒触发时应用未运行 | 下次启动补扫，只提醒一次，不批量轰炸 |
| 迁移重复执行 | 幂等：已迁移过则提示"已完成迁移，是否查看结果"，不重复建任务 |
| schedules 数据量大 | 迁移采用分批执行 + 进度提示 |
| 数据库写入失败 | 提示并保持原状态（事务回滚） |

---

## 16. 验收标准

### 16.1 P0 验收（闭环）

1. 可创建/编辑/归档项目；项目列表按状态页签查看；卡片展示统计与进度；
2. 可创建/编辑/删除任务；任务必属项目；删除进回收站可恢复；
3. 看板默认三列；跨列拖拽改状态、同列拖拽排序；拖到已完成记录完成时间，拖回清空；失败回滚；
4. 任务可标记完成/重新打开；完成时间正确；
5. 今日任务正确聚合：今天到期 / 已逾期 / 计划今日 / 进行中高优；
6. "计划今日"在自然日切换后正确清理；顺延操作正确；
7. 项目进度统计实时正确（含逾期判断）；
8. 删除项目连带任务进回收站，恢复后状态与归属正确；
9. 逾期任务有明确视觉标识。

### 16.2 P1 验收（增强）

1. 搜索覆盖标题/描述/备注/标签/项目名，支持高亮与二次筛选；
2. 看板与列表视图可切换，列表筛选/排序正确；
3. 标签可新建/编辑/删除/配色；删除标签后任务关联正确清理；按标签筛选正确；
4. 截止提醒、逾期提醒、每日待办提醒按时触达（站内 + 本地通知）；点击通知进入任务详情；已完成/已删除不提醒；单任务与全局开关生效；
5. 复制任务、移动到其他项目正确。

### 16.3 迁移验收

1. 迁移入口一次生成默认项目与任务，数量与源 schedules 一致；
2. 已勾选完成日程迁移为"已完成"且完成时间正确；
3. 重复执行迁移不产生重复数据；迁移前有备份提示。

### 16.4 数据验收

1. 创建/完成/更新时间记录正确；
2. 进度计算、逾期判断正确；
3. 软删除/恢复状态正确；回收站彻底删除后数据不可恢复（二次确认）。

---

## 17. 里程碑计划（结合 TimeWrite 版本）

| 版本 | 内容 | 出口标准 |
| ---- | ---- | -------- |
| 本期（V 下一版，建议 v1.5.0） | P0 + P1 全部 + 日程迁移入口 + 旧日程 UI 保留（降级提示） | 16.1/16.2/16.4 验收通过；迁移可用 |
| 下一版（v1.6.0） | 移除 schedules 旧 UI 与表；回收站自动清理；附件（12.4） | 旧数据清理完成、用户反馈无回退诉求 |
| 后续版本 | 子任务/清单、重复任务、周报复盘、日历视图（原 6.3 各项） | 按优先级逐项排期 |

---

## 18. 风险与注意事项

| 风险 | 影响 | 应对方式 |
| ---- | ---- | -------- |
| 功能过重 | 个人使用压力大 | P0/P1 收敛，P2 明确后置 |
| 字段过多 | 创建门槛高 | 快速新建仅标题必填，其余详情页补 |
| 状态过多 | 维护成本高 | 固定三态 |
| 提醒打扰 | 厌烦关闭 | 全局开关 + 逾期每日一次 + 每日待办默认关 |
| 与"个人日程"双轨并存期混乱 | 用户不知用哪个 | 入口降级提示 + 迁移引导 + 下一版下线（6.4） |
| 迁移数据损坏 | 用户既有数据丢失 | 迁移前自动备份、幂等、结果报告 |
| 项目/任务过多 | 列表变慢 | SQLite 索引 + 分页/虚拟列表（列表视图） |
| 坚持使用难 | 频率低 | 强化今日任务、快捷创建、逾期高亮 |
| 数据丢失 | 信任下降 | 软删除 + 回收站 + 纳入全量备份 |

---

## 19. 本期 MVP 范围汇总（P0 + P1）

```text
对象：项目 · 任务卡 · 标签 · 今日任务 · 回收站
状态：待办 · 进行中 · 已完成
页面：今日任务 · 项目列表 · 项目详情（看板/列表）· 任务详情(弹层) · 搜索 · 回收站 · 设置(提醒/标签/迁移)
能力：创建项目 · 创建任务 · 看板拖拽 · 完成/重开 · 计划今日 · 今日任务
　　　搜索筛选 · 标签管理 · 截止提醒(站内+本地通知) · 复制/移动任务 · 删除回收 · 项目进度统计
暂不做：多人协作 · 评论 · 子任务 · 重复任务 · 附件 · 富文本备注 · 日历视图 · 自动清理 · 云同步
```

---

## 20. 待确认问题

1. 任务描述是否需要完整富文本编辑（本期建议复用 TipTap 但仅暴露轻量能力，如纯文本 + 换行）；
2. 截止时间是否设为任务必填（影响"今日任务"与逾期统计的覆盖面，建议可选但创建时引导填写）；
3. "进行中的高优先级任务展示进今日任务"默认开启是否合适（建议开启，可在设置关闭）；
4. 项目页签是否需要"已归档"独立展示（本期建议保留，成本低）；
5. 周报/复盘视图（统计图表）的优先级（P2 里先做哪一个）；
6. 迁移后旧"个人日程"入口的降级方式（角标提示 / 弹窗引导二选一）。
