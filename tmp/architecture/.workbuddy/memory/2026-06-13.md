# 2026-06-13 工作记录

## 架构文档 P0 审计修改

对 MirageInk 项目的全套 11 份架构文档进行了审计，并完成了全部 7 个 P0（Critical）问题的修改：

### 已修改文档

1. **03-多窗口Store隔离与状态管理.md**
   - 新增 3.3.4 Store 清理钩子设计（`__TAURI_STORE_CLEANUP__`）
   - 修改 3.4 Store 生命周期，增加强制清理步骤
   - 修改 3.8 窗口关闭清理逻辑，增加 thunk 取消机制
   - 新增 3.8.1 Event Listener 取消注册规范

2. **05-Python-AI扩展架构.md**
   - 新增 5.3.4 心跳机制（孤儿进程防护）：Python↔Rust 双向检测
   - 新增 5.4.3 Bridge 认证机制：随机 Token + X-Auth-Token header

3. **06-安全架构设计.md**
   - 重写 6.5.1 数据库加密：明确 SQLCipher 全库加密 + PBKDF2 密钥派生
   - 新增 6.4.4 Bridge 回调认证引用
   - 新增 6.3.4 RBAC 数据完整性保护（HMAC 签名）
   - 新增 6.3.5 权限变更审计日志

4. **07-并发控制与数据一致性.md**
   - 修正 7.5.1 version 字段从 `u32` → `u64`
   - 修正 7.5.2 冲突检测代码，增加溢出保护检查
   - 补充数据库端 BIGINT 类型说明

5. **08-事务管理与乐观更新.md**
   - 修正 8.4.2 `client_version: u32` → `u64`
   - 增加版本溢出防御性检查
   - 新增 8.3.1 TypeScript 版本号精度说明

6. **09-离线缓存与冲突解决.md**
   - 重写 9.5.1 策略矩阵：整行 LWW → 字段级 LWW
   - 重写 9.5.2 实现：`resolve_field_level_lww` 按字段独立比较时间戳

7. **10-测试策略.md**
   - 修改 10.1 测试金字塔：E2E 从 5% → 15%
   - 新增 10.1.1 测试分层说明表
   - 新增 10.4.1 E2E 场景矩阵（7 个场景）
   - 新增 10.4.3 关键场景 E2E-01 完整测试代码
   - 新增 10.4.4 E2E CI 集成配置
   - 修改 10.7 覆盖率目标：明确 E2E 关键路径清单

## 架构文档 P1（Important）修改

完成了全部 5 个 P1 问题的修改：

8. **02-MVVM分层详解.md**
   - 修正 Domain struct `version: u32` → `u64`，与 07/08 一致
   - 修正 UseCase `expected_version: u32` → `u64`
   - 修正 Infrastructure Repository `expected_version: u32` → `u64`
   - 增加 `checked_add(1)` 溢出保护
   - 新增 2.4 分层映射 `u64/i64 ↔ number` 规则 + JS 安全边界说明
   - 2.2.4 TS DTO 增加 JS `Number.MAX_SAFE_INTEGER` 安全边界的代码注释

9. **01-总体架构设计.md**
   - 修正 1.6 LWW 决策描述：`Last-Write-Wins + 手动` → `字段级 Last-Write-Wins + 手动`
   - 细化 1.7 冷启动时间：标注 PyInstaller 打包模式 <2s / 开发模式 <4s
   - 更新 1.7 数据安全描述：`SQLCipher AES-256 + PBKDF2 密钥派生`

10. **11-CICD持续集成与交付.md**
    - CI 流水线新增 `test-e2e` job（WebDriver 多窗口场景）
    - Quality Gate 增加 `test-e2e` 依赖
    - 11.5.2 增加 Bridge Auth Token 环境注入说明（引用 05 §5.4.3）
    - 11.8.1 sqlcipher feature 增加 PBKDF2 描述
    - 更新 11.11 时间预算：增加 E2E 测试行

11. **04-事件系统与跨窗口通信.md**
    - 4.1 通信体系增加 Bridge Event（Rust↔Python Auth Token）行
    - 4.2.4 事件目录新增 `agent:error`、`sync:error`、`bridge:auth-error`、`sync:offline:queue:synced`、`system:window:closed`、`system:python:health`
    - 4.8 故障模式新增"监听器意外断连"和"Bridge Auth 失败"行

12. **00-README.md**
    - 修正文档数量：`12个专题` → `11个专题`
    - 术语约定新增"字段级 LWW"定义
    - Bridge 术语补充 Auth Token 保护说明

## 架构文档 P2（Nice-to-have）修改

完成了全部 5 个 P2 问题的修改：

13. **05-Python-AI扩展架构.md**
    - 5.3.3 优雅关闭增强：新增请求排空（Draining）阶段，Drain → SIGTERM → wait(10s) → SIGKILL
    - 新增 5.3.5 Agent 健康指标暴露：Prometheus 格式指标（内存/CPU/QPS/P99延迟/错误率）
    - 新增 5.3.6 模型配置管理：YAML 配置结构，Skill→模型路由，API Key 环境变量注入
    - Rust 侧指标采集 Monitor 模块

14. **06-安全架构设计.md**
    - 新增 6.3.6 审计日志生命周期管理：retention_days=90 / max_rows=100K
    - 两种清理策略（按时间 + 按数量）+ 归档模式（JSON 导出）
    - 审计日志表索引补充（created_at + user_id）
    - GDPR/个人信息保护法合规提示

15. **09-离线缓存与冲突解决.md**
    - 新增 9.7.1 前端缓存淘汰策略：LRU / max_size / TTL 三层规则
    - IndexedDB 缓存管理器实现（按条目数 + 总大小淘汰）
    - 新增 9.7.2 Rust 侧 SQLite 缓存表清理（队列 + 冲突记录 + VACUUM）
    - 新增 9.8 离线队列完整生命周期（入队→重试→成功→丢弃）+ 幂等性保证
    - 新增 9.8.1 幂等性实现：UUID v7 client_id + LruCache 1000 去重

16. **02-MVVM分层详解.md**
    - 新增 2.6 React Error Boundary 策略：三级边界（组件级→区域级→窗口级）
    - ErrorBoundary 类实现 + 使用示例
    - 新增 2.7 IPC 故障降级与离线指示器：useIPCStatus hook + OfflineIndicator 组件

17. **04-事件系统与跨窗口通信.md**
    - 新增 4.9 事件重放与恢复机制：三级恢复（增量轮询→聚焦恢复→事件重放）
    - EventReplayBuffer（Rust 环形缓冲区 + 前端消费逻辑）
    - fullVersionDiff 增量同步
    - 新增 4.10 最终一致性校验：跨窗口 Version Gap 检测 + Auto Fix
    - 一致性校验触发时机表（聚焦/定时/保存前/手动）

---

### 三轮修改总览

| 优先级 | 修改数 | 涉及文档 |
|--------|--------|---------|
| P0 (Critical) | 7 | 03, 05, 06, 07, 08, 09, 10 |
| P1 (Important) | 5 | 00, 01, 02, 04, 11 |
| P2 (Nice-to-have) | 5 | 02, 04, 05, 06, 09 |
| **合计** | **17** | **11/11 全覆盖** |

---

## 统一错误处理模型（架构评审后续）

基于架构审计建议"缺少统一错误处理架构"，在文档中实现了跨语言（Python → Rust → TypeScript）的统一错误传播协议：

### 修改内容

18. **02-MVVM分层详解.md**
   - 新增 §2.8 统一错误处理模型（约 350 行）
   - §2.8.1 问题与目标：三层语言栈各自错误模型缺乏统一传播
   - §2.8.2 错误码分类表：9 大类（AUTH_*/VALIDATION_*/VERSION_*/NOT_FOUND/IPC_*/DB_*/AGENT_*/SECURITY_*/INTERNAL）
   - §2.8.3 Rust AppError 定义：5 字段（code/message/severity/recoverable/detail）+ 工厂方法 + From trait + from_agent_response 解析器
   - §2.8.4 TypeScript ApiError：parseApiError() / getErrorAction()（toast/modal/inline/silent/relogin）+ useErrorHandler hook
   - §2.8.5 错误传播全链路图：Python AgentError → HTTP → Rust AppError → Tauri invoke → TS ApiError
   - §2.8.6 向后兼容策略：compat_string_error! 宏 + 3 步迁移路径
   - 修正 Command Handler 示例：旧方案 Result<T, String> vs 新方案 Result<T, AppError>

19. **05-Python-AI扩展架构.md**
   - 新增 §5.9 统一错误传播协议（约 150 行）
   - §5.9.1 AgentError Pydantic 模型：与 Rust AppError 字段一一对应
   - §5.9.2 LLM 错误子类型工厂：llm_timeout / rate_limited / token_limit_exceeded / model_unavailable / invalid_api_key / internal
   - §5.9.3 路由层集成：拦截 OpenAI SDK 异常→AgentError→HTTPException
   - §5.9.4 模型切换降级策略：CLOUD↔LOCAL FALLBACK_CHAIN，解决 POLISH 固定 Ollama 不可用时无降级的问题
   - §5.5.1 路由图增加降级引用

20. **00-README.md**
   - 02 文档关注点补充"统一错误处理"
   - 05 文档关注点补充"错误传播协议"

21. **01-总体架构设计.md**
   - §1.7 非功能需求新增"错误处理"行，引用 02 §2.8 和 05 §5.9

---

## 可观测性与数据埋点方案（新增第 12 号文档）

基于架构审计发现"缺少应用埋点数据采集和日志存储和发送到服务端的文档"，创建了完整的可观测性方案。

### 新增内容

| # | 文档 | 变更 |
|---|------|------|
| 22 | **12-可观测性与数据埋点.md**（新建） | 约 600 行完整方案 |
| 23 | 00-README.md | 专题数 11→12，新增文档索引 + 阅读路径 |
| 24 | 01-总体架构设计.md | §1.7 NFR 新增"可观测性"行 |
| 25 | 04-事件系统与跨窗口通信.md | 通信体系新增 Telemetry Event 行 |
| 26 | 05-Python-AI扩展架构.md | §5.3.5 增加与业务埋点差异化说明 |
| 27 | 06-安全架构设计.md | §6.3.6 增加审计日志与埋点分工说明 |
| 28 | 02-MVVM分层详解.md | §2.6 ErrorBoundary 引用 12 |

### 12 号文档章节结构

- §12.1 设计目标与原则（4 条）
- §12.2 领域建模：统一事件模型（5 类 + 日志分级 + TelemetryEvent 结构 + 命名规范）
- §12.3 三层埋点架构：TS Tracker SDK / RTK Middleware / Rust tracing + Command Timer / Python stdout JSON
- §12.4 本地日志存储：SQLite 表 + 文件轮转 + 配额淘汰
- §12.5 远程上报：HTTPS 协议 + 批量策略 + 指数退避重试 + 离线缓冲
- §12.6 隐私与合规：数据分级（允许/禁止）、脱敏规则、用户控制台
- §12.7 服务端接收方案（指引）
- §12.8 与现有系统的关系表
- §12.9 实施 Checklist（3 阶段 22 项）
