# pnpm 配置 - MirageInk (Tauri v2)

# 禁止提升所有依赖，避免 Tauri 的 native 模块解析问题
node-linker=hoisted

# 解决 Tauri CLI 中某些 optional peer deps 警告
auto-install-peers=true

# 禁止安装时修改 lockfile（CI 保护）
# ci=true  # 取消注释可在 CI 中使用

# shamefully-hoist 确保 Vite 插件等能找到 peer deps
shamefully-hoist=true
